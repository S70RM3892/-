# 出所と再現性

このリポジトリの数字がどの一次資料から来ているかの対応表。
`README.md` の主張は、ここに載っていない入力には依存しないこと（例外は 1 件、末尾に明記）。

## 一次資料

| ファイル | 中身 | 出所 | 検証状況 |
|---|---|---|---|
| `data/r9.txt` | 令和9年度 募集要項の全文テキスト | 京大公式 PDF を `raw/kx.py`（pdfminer 座標抽出）でテキスト化 | 工学部の配点表（共テ 25/50/25/25/50/50=225、個別 100/250/250/200=800、計 1025）と第1段階選抜配点（200/100/200/200/200/100=1000）、受入学生数目安（地球工175 建築77 物理工225 電電128 情報94 理工化215＝914）を本文で確認済み |
| `data/r6senbatsu.txt` | 令和6年度 選抜要項 | 同上 | 受入学生数目安 情報学科 87 人を確認（令和9 の 94 人と比較するために保持） |
| `data/kyodai_official.json` | 学部・学科別の配点と合格者最低点 8 年分（`last`、新しい年が先頭） | 京大公式の入試データ | `analysis/build_dep.py` で `dep.json` を再生成して照合 |
| `data/ratio.json` | 学科×年の志願倍率 2017–2026 | 同上 | 同上 |
| `analysis/dist_r6/7/8.csv` | 大学入試センター 科目別成績分布（全国・本試験、令和6/7/8年度） | 大学入試センター公表 | cp932。`kt.py` の `load()` が科目名でパース。科目名が変わると落ちるよう例外にしてある |
| `data/q_r8.csv` | 令和8年度 設問別得点率及び正答率 | 大学入試センター公表 | `irt.py check_max()` で各科目の配点合計＝公表満点を assert |

## 派生ファイル

`analysis/dep.json`（6 学科 × 8 年）は一次資料から再生成できる。

```bash
cd analysis && python3 build_dep.py        # 検算のみ
cd analysis && python3 build_dep.py --write # 再生成
```

- `rate` = `last / 満点(年) * 100`。**満点は 2019–2024 が 1000 点、2025 以降が 1025 点**
  （新課程で共通テスト『情報Ⅰ』50 点が加わったのは令和7年度入試＝2025 年から）。
  この regime break が 8 年パネルの最大の構造断層で、現行配点のデータはまだ 2 年しかない。
- `dv` = `rate` の当年 6 学科平均からの乖離。**構成上 6 学科で必ずゼロ和**になるので、
  6 系列は独立標本ではない（`rb.py` の 1.）。
- `ratio` = `ratio.json` そのまま、`dr` = その年内乖離。
- 検算の残差は最大 0.005pt（1025 点中 0.05 点）で、これは `dep.json` 側の丸め。

### 未解決の出所

- `dep.json` の `app`（志願者数）と `acc`（席数）は一次資料がこのリポジトリに無い。
  `acc` は `sd2.py` / `sd3.py` で合格者数として順序統計 SD に使われているが、
  募集要項の受入学生数目安（令和6 情報 87 人）とは 1–2 人ずれる年がある。
  特色入試の残余が前期に加算されるためと思われるが、確認していない。
- `analysis/sd7.py` の `sT=65.4` と `muT=-0.553*sT` は**このリポジトリのどの資料からも出てこない**。
  第2志望ラダーの確率の水準はこの 2 定数がほぼ全部を決めている（`rb.py` の 5.）。
  順序（理工化 > 地球工 > 建築 > 電電 > 物理工）は定数に依らず不変なので、
  主張に使ってよいのは順序だけ。

## 未整理

`raw/` は収集時のスクレイプとワンショットのスクリプトをそのまま置いた作業場で、
どれがどの成果物を作ったかの対応は取れていない。`analysis/` と `data/` だけが再現対象。
