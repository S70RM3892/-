# 改訂記録

## 2026-09-11 — 批評を受けての全面改訂

### スクリプトの対応

| 旧 | 新 | 備考 |
|---|---|---|
| `sd.py` `sd2.py` `sd3.py` `sd4.py` `sd5.py` | `p01_panel.py` | 統合。壊れた検定2本を置換（下記） |
| `sd6.py` `sd7.py` | `p02_gap_ladder.py` | 分散の式を修正 |
| `kt.py` | `p03_ct_weights.py` | |
| `kt2.py` | `p04_ct_variance.py` | 切断点感度を追加 |
| `irt.py` `irt_run.py` | `kyodai/irt.py` + `p05_irt_ct.py` | 模型と実行を分離 |
| — | `p06_niji.py` | 新規。二次800点 |
| — | `p07_budget.py` | 新規。点あたり時間 |
| — | `p08_ratio.py` | 新規。between/within 分解 |
| — | `p09_rejected.py` | 新規。棄却仮説の再現コード |
| — | `p10_validate.py` | 新規。データ検証 |

### 直したバグ

1. **`sd4.py` の並べ替え検定が壊れていた。** 学科平均を除去せずに年内シャッフル
   していたため帰無の分散が過大になり、`p=0.9688` を返していた。README は
   `sd5.py` の `p=0.0041` だけを載せており、リポジトリを clone して `sd4.py` を
   走らせた人は矛盾する数字を見ることになっていた。
2. **`sd2.py` の片側 p の符号取り違え。** 観測された `r=−0.875` に対し
   「r 以上」で数えて `p=0.9889` を出していた。
3. **`sd7.py` の分散が誤り。** `T+gap = 得点 − cutoff_相手` なので情報の最低点は
   相殺されるのに、ギャップのSDを足していた（その67–95%は情報自身の変動）。
4. **`kyodai_official.json` の `last` は新しい年が先頭。** 素直に years と zip すると
   年が逆順になる。この改訂作業中に実際に踏んだ。
   `datasets.official_cutoffs()` で並べ替え＋dep.json との照合を強制し、
   `tests/test_data.py::test_official_cutoffs_are_newest_first` で固定した。
5. **`exec(open(x).read().split(...))` による他ファイル取り込みを廃止。**
   文字列分割での再利用は、片方を編集した瞬間に静かに壊れる。

### 取り下げた主張

| 旧 README の記述 | 現状 |
|---|---|
| 「情報学科の最低点は6学科で突出して不安定」 | 旧配点期間(2019–24)のみでは F=0.68, p=0.71。2025年の段差が残差分散の71.5%を説明。主張を「制度変更の影響を最も強く受けた学科」に書き換え |
| 「志願倍率が最低点の70%を説明」 | within r = −0.178（符号が逆）。倍率を足すと LOO MAE は悪化。between のみの現象 |
| 「得点開示は工学部分が11件」 | 11は全学部合計。工学部は4件（うち1件は汚染行） |
| 「第1段階選抜は10年で7人(0.18%)」 | 根拠データがリポジトリに無い。数値を削除し条文のみ残す |
| 「割り振りは serial dictatorship」 | 募集要項に記載なし。作業仮説に格下げ |
| 「公開情報のみを一次資料から収集・検証」 | 難易度評価の出典は個人のnote記事とZ会。`raw/PROVENANCE.md` で区分 |
| 「ユーザー自身の模試成績は結論に使っていない」 | `sT`/`muT` が個人パラメータだった。`params.json` で status=USER と明示 |
| 「合計16.6–23.7点」を主要結果として提示 | z を得点率に合わせた時点で合計は算術的に決まる。推定量ではない |
| 確率を 0.1% 刻みで表示（53.0% 等） | 帯表示に変更。1%刻み |

### 追加した検証

- `p10_validate.py`: 配点の縦計、パネルの整合、分布CSVの度数からの再計算、
  設問配点、得点開示の汚染検出。不整合があれば exit 1。
- `tests/`: 43件。統計ユーティリティ、模型の不変量（周辺確率が全国得点率に戻る）、
  データ不変量、README 数値のスナップショット。
- `run_all.sh`: 検証 → 全解析 → テスト を一括実行。

### 追加した分析

- **二次800点の定量模型**（`p06_niji.py`）。旧版は全体の78%を占める二次に
  定量模型を持っていなかった。
- **点あたり時間**（`p07_budget.py`）。旧版は「点がどこにあるか」だけを測り
  「どこの点が安いか」を測っていなかった。
- **z の外部固定と感度**（`p05_irt_ct.py`）。旧版は z=+2.0 を根拠なく決め打ちしていた。
- **局所独立の緩和**（`kyodai/irt.py` の `rho`）。
- **較正 r のバイアス診断**。corr(r, 取りこぼし) = −0.49、
  corr(r, 上側の当てはめ誤差) = −0.78 を明示した。

### リポジトリ衛生

- `LICENSE` を追加（コードは MIT、第三者データは対象外）。
- `raw/PROVENANCE.md` に全資料の出所・権利・公開前チェックリスト。
- `.gitignore` から `*.pdf` の除外を解除。旧版は README が「r9.pdf より座標抽出」と
  書いているのに一次資料がリポジトリに無い状態だった。
- README の見出し番号の重複（`### 5.` が2つ）を修正。

## 2026-09-11 (2) — 未利用情報の探索

「結局一般の受験生が考えることと変わらない」という指摘を受けて、
**なぜ何も出なかったのか**から探索範囲を決め直した。

### 探索の原則

前回の分析は全て「公開されている集計量（合格最低点・志願倍率・全国平均点）」を
使っていた。それは全受験生・全予備校が見ているので、優位が残っているはずがない。
残っているとすれば次の3か所:

1. 処理コストが高くて誰も触らないデータ（設問別得点状況の未使用列）
2. 出願前ではなく **試験室の中** の意思決定
3. 過去データに原理的に含まれない **前向き情報**

### 潰した筋（記録として残す）

| 調べたこと | 結果 |
|---|---|
| `q_r8.csv` の未使用列 `正答率` | 得点率との差＝部分点。理系科目は全科目でゼロ（完全に全か無か）。情報Ⅰで1.31素点のみ。**空振り**。ただし IRT のベルヌーイ扱いが正しいことの確認にはなった |
| 数学ⅡBC の選択問題どれを選ぶか | 4通りの組合せの期待素点差は 1.0素点 = **0.13点/1025**。σ の 0.01〜0.02倍。**空振り** |

### 見つかったもの

1. **募集人員の付け替え**（`p11_seats.py`）。情報学科は 87人(令和6) → 94人(令和9) の
   +8.0%。総定員はほぼ不変で、理工化・地球工・物理工から情報・電電へ移っている。
   最低点への機械的効果は情報 −4.4点 / 理工化 +5.8点。
   第2志望ラダーの8年平均ギャップは 3〜14% 過大だった（順序は不変）。
   合格最低点を決める4要素のうち出願前に公開されるのは定員だけなので、
   これは過去データからは原理的に出てこない。

2. **自分の得点分散の非対称性**（`p12_risk.py`）。全不確実性のうち
   合格最低点側は 1.7–11.2% しかない。この研究は8年かけて小さい方を測っていた。
   大きい方（自分の当日ゆらぎ）を見ると `∂P/∂s = −φ(μ/s)·μ/s²` で符号が μ で反転する:
   **ラインより下にいる人は得点の分散を上げるべきで、上にいる人だけが下げるべき**。
   μ=−36点で sp を 64.5→45 に下げると合格確率は 7.3ポイント下がる。
   交換レートは `許容できる期待値の損失 = |z| × 分散の増加分` という形になる。
   受験の標準的助言（ミスをなくせ・確実に取れ）は合格者＝μ>0 から抽出されているため、
   生存者バイアスで向きが歪んでいる。

   この結果は μ の**符号**だけで向きが決まるので、最も不確かな量（μ, sp の大きさ）に
   対して頑健である。この研究で唯一まともに使える結果。
